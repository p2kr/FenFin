# Database credentials
DB_USER=expenses
DB_PASSWORD=your_secure_local_password
DB_NAME=expenses

# Volume path for postgres (defaults to 'pgdata' if using standard named volume locally)
DB_VOLUME_PATH=pgdata
